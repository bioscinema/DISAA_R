% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize_parameters.R
\name{initialize_parameters}
\alias{initialize_parameters}
\title{Initialize Parameters for the MZINB Model}
\usage{
initialize_parameters(counts, covariates)
}
\arguments{
\item{counts}{A matrix of count data (observations by features).}

\item{covariates}{A matrix of covariates (observations by covariates).}
}
\value{
A list containing initialized values for the parameters of the MZINB model:
\describe{
\item{data}{The count data matrix (n x m).}
\item{X}{The covariate matrix (n x p).}
\item{n}{Number of observations.}
\item{m}{Number of features (columns in the count matrix).}
\item{p}{Number of covariates.}
\item{pi}{A vector of probabilities of zero inflation, initialized to 0.5 for each feature.}
\item{eta}{A vector of median values of the counts, used to initialize the latent variable for each observation.}
\item{beta}{A matrix of regression coefficients, initialized to 1.}
\item{mu}{A matrix of expected counts, initialized to 0.}
\item{theta}{A vector of dispersion parameters, initialized to 1.}
\item{v}{A matrix indicating whether counts are zero (binary indicator).}
\item{tau}{A matrix for latent variables indicating zero inflation (initialized to same as \code{v}).}
}
}
\description{
This function initializes the parameters required for the MZINB (Multivariate Zero-Inflated Negative Binomial) model.
It takes as input a matrix of count data and a matrix of covariates, and initializes values such as
latent variable matrices, regression coefficients, and other model parameters.
}
\examples{
counts <- matrix(rpois(20, lambda = 5), nrow = 5, ncol = 4)
covariates <- matrix(rnorm(20), nrow = 5, ncol = 4)
params <- initialize_parameters(counts, covariates)
}
